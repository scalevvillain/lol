
int task1(){
 int a; char c;
 puts("введите количество символов и символ через пробел"); scanf("%d%c", &a, &c);
 for (int start = 1; start <= 1; start++); putchar("+");  for (int start = 1; start <= a; start++); putchar(c);
   for (int start = 1; start <= a; start++); putchar("+");
